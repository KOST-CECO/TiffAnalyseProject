Anleitung zu GIT 

# GIT: Settings für Kommandozeile
git config -l
git config user.name mkaiser56
git config user.email m.kaiser@access.ch
git config core.editor pspad.cmd
git config http.proxy proxy.edi.admin.ch:8080

SET TERM=xterm

# Neues Arbeitsverzeichnis anlegen
git clone https://github.com/KOST-CECO/csv2siard.git _workbench
cd _workbench

# Repository überprüfen
gitk
git status
git add
git commit –a
git push

# file .gitignore
Thumbs.db
*.lnk
*.exe
